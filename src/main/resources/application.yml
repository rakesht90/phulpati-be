spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/phulpati_v1
    username: phulpati
    password: phulpati
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect